

////-------------------*\
//// Header Size and such
////-------------------*/

header.headerMobile
  z-index: 50
  cf()
  height: 50px
  display: none;
  +below(2)
    display: block;
    section
      span(3/4)
    aside
      span(1/4)

////-------------------*\
//// Color
////-------------------*/
header.headerMobile  
  background: white;





header.headerMobile.main-header
  section
    h1
      @extend .reset;




////-------------------*\
//// Button Style
////-------------------*/

/* ========= Menu Icon Base ======== */

header.headerMobile
  aside
    #hamburger
      cf()
      z-index: 100;
      background: green
    	height: 60px;
    	cursor: pointer;
    	display: inline-block;
    	font-size: 10px;
    	position: relative;
    	-webkit-user-select: none;
    	-moz-user-select: none;
    	-ms-user-select: none;
    	user-select: none;
    	-webkit-transition: 0.2s ease-in-out;
    	transition: 0.2s ease-in-out;
    	-webkit-tap-highlight-color: transparent;

#hamburger span {
  width: 70px
  background: #222222;
  border-radius: 2em;
  -webkit-backface-visibility: hidden;
  backface-visibility: hidden;
  height: 0.8em;
  position: absolute;
  -webkit-transition: 0.2s ease-in-out;
  transition: 0.2s ease-in-out;
  -webkit-tap-highlight-color: transparent;
}

/* ============ MenuIcon ============ */
#hamburger span {
  left: 0;
}

#hamburger span:nth-child(1) {
  top: .5em;
}

#hamburger span:nth-child(2) {
  top: 2.1em;
}

#hamburger span:nth-child(3) {
  top: 3.7em;
}

.open#hamburger span:nth-child(1) {
  -webkit-transform: rotate(45deg) translate(1.1em, 1.1em);
  -ms-transform: rotate(45deg) translate(1.1em, 1.1em);
  transform: rotate(45deg) translate(1.1em, 1.1em);
}

.open#hamburger span:nth-child(2) {
  opacity: 0;
}

.open#hamburger span:nth-child(3) {
  -webkit-transform: rotate(-45deg) translate(1.1em, -1.1em);
  -ms-transform: rotate(-45deg) translate(1.1em, -1.1em);
  transform: rotate(-45deg) translate(1.1em, -1.1em);
}






//----------------------------------*\
// Nav setting
//----------------------------------*/



$sliding-menu-border-color = $g1;
$sliding-menu-background = lighten($sliding-menu-border-color, 5);
$sliding-menu-color = white;
$sliding-menu-border = 2px solid $sliding-menu-border-color;
$sliding-menu-background-hover = darken($sliding-menu-border-color, 5);
$sliding-menu-color-hover = darken($sliding-menu-border-color, 15);



.sliding-menu-content {
  border: 3px solid darken($sliding-menu-border-color, 5);
  position: fixed;
  top: 0px;
  right: auto;
  bottom: 0px;
  left: 0px;
  width: 220px;
  height: 100%;
  transform: translateX(-220px);
  transition: all .25s linear;
  background: $sliding-menu-background;
  z-index: 999999;
  overflow-y: none;
  -webkit-overflow-scrolling: touch;
  li {
    stack()
    height: 75px;
    border-bottom: $sliding-menu-border;
  }
  li a {
    font-size: 2em;
    color: $sliding-menu-color;
    display: block;
    font-weight: bold;
    padding: 1em;

    &:hover {
      // background-color: $sliding-menu-background-hover;
      // color: $sliding-menu-color-hover;
    }
  }

  &.is-visible {
  transform translateX(0);
  }
}

.menu-screen {
  position: fixed;
  top: 0px;
  right: 0px;
  bottom: 0px;
  left: 0px;
  transition: all 0.15s ease-out 0;
  background: $sliding-menu-border-color;
  opacity: 0;
  visibility: hidden;
  z-index: 999998;

  &.is-visible {
    opacity: .4;
    visibility: visible;
  }
}